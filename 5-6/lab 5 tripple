
public class TrippleDice extends Dice
{
	
	int x;
	
	public TrippleDice() {
		;
	}

	@Override
	public int Throw()
	{
		
			int die = (int) (1 + (Math.random()*6));
			return die;
	
	}
}

// YOu will need to call the Dice constructor to initialize 3 dices, or
// the Throw method will need to call the base Dice.Throw with proper parameter to represents the 3 dices.
